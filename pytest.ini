[pytest]
testpaths = tests
filterwarnings =
    error
    # pandas 2.2.x emits this internally from resample("W"); not our code.
    ignore:.*generic.*unit for NumPy timedelta is deprecated.*:DeprecationWarning
